apiVersion: v1
kind: Service
metadata:
  name: frontend-api
  labels:
    app: frontend-api
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: frontend-api

